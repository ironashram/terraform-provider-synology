---
page_title: "Core: synology_core_reverse_proxy"
subcategory: "Core"
description: |-
  Manages a DSM reverse-proxy rule.
  This resource models a single host-based DSM reverse-proxy entry. It is a good fit for
  Vault, MinIO, and similar services that sit behind the Synology reverse proxy.
  Certificate issuance and certificate-to-service binding remain separate concerns.
  Example Usage
  
  resource "synology_core_reverse_proxy" "vault" {
    name = "vault"
  
    frontend = {
      host     = "vault.synology.example.com"
      protocol = "https"
      port     = 443
      hsts     = true
    }
  
    backend = {
      host     = "127.0.0.1"
      protocol = "http"
      port     = 8200
    }
  }
---

# Core: Reverse Proxy (Resource)

Manages a DSM reverse-proxy rule.

This resource models a single host-based DSM reverse-proxy entry. It is a good fit for
Vault, MinIO, and similar services that sit behind the Synology reverse proxy.

Certificate issuance and certificate-to-service binding remain separate concerns.

## Example Usage

```hcl
resource "synology_core_reverse_proxy" "vault" {
  name = "vault"

  frontend = {
    host     = "vault.synology.example.com"
    protocol = "https"
    port     = 443
    hsts     = true
  }

  backend = {
    host     = "127.0.0.1"
    protocol = "http"
    port     = 8200
  }
}
```



<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `backend` (Attributes) Upstream target definition. (see [below for nested schema](#nestedatt--backend))
- `frontend` (Attributes) Public-facing listener definition. (see [below for nested schema](#nestedatt--frontend))
- `name` (String) Rule name shown in DSM (stored as the entry description on DSM 7).

### Optional

- `customize_headers` (Attributes Set) Custom request headers forwarded by DSM. (see [below for nested schema](#nestedatt--customize_headers))
- `proxy_connect_timeout` (Number) Upstream connect timeout in seconds. Default: `60`.
- `proxy_http_version` (Number) Upstream HTTP version. Default: `1`.
- `proxy_intercept_errors` (Boolean) Whether DSM should intercept upstream errors. Default: `false`.
- `proxy_read_timeout` (Number) Upstream read timeout in seconds. Default: `60`.
- `proxy_send_timeout` (Number) Upstream send timeout in seconds. Default: `60`.

### Read-Only

- `id` (String) DSM reverse-proxy rule UUID.

<a id="nestedatt--backend"></a>
### Nested Schema for `backend`

Required:

- `host` (String) Upstream hostname or IP.
- `port` (Number) Upstream port.

Optional:

- `protocol` (String) Upstream protocol. Valid values are `http` and `https`. Default: `http`.


<a id="nestedatt--frontend"></a>
### Nested Schema for `frontend`

Required:

- `host` (String) Frontend hostname served by DSM.

Optional:

- `hsts` (Boolean) Whether to enable HSTS for HTTPS listeners. Default: `true`.
- `port` (Number) Frontend port. Default: `443`.
- `protocol` (String) Frontend protocol. Valid values are `http` and `https`. Default: `https`.


<a id="nestedatt--customize_headers"></a>
### Nested Schema for `customize_headers`

Required:

- `name` (String) Header name.
- `value` (String) Header value.